#include <iostream>
using namespace std;

int n;

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	cin >> n;

	if (n == 1) {
		cout << -1;
		return -1;
	}
	for (int i = 1; i * i <= n; i++) {

	}
}