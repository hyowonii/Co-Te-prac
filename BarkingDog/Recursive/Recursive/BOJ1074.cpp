#include <iostream>

using namespace std;

int N, r, c;

int recursion(int N, int r, int c) {
	// basic condition
	if (N == 0) return 0;


}

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	cin >> N >> r >> c;

	cout << recursion(N, r, c);
}