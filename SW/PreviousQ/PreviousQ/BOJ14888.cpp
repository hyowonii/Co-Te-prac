#include <iostream>
#include <vector>
#include <stack>

using namespace std;

int N;
int num[15];
int op[4];		// +, -, *, /

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	// ют╥б
	cin >> N;
	for (int i = 0; i < N; i++) {
		cin >> num[i];
	}	
	for (int i = 0; i < 4; i++) {	
		cin >> op[i];
	}
	//



}